#include "main.h"
#include <string.h>

/**
 * _strcmp - fucntion that compares two strings
 * @s1: string 1 to compare
 * @s2: string 2 to campare
 * Return:  0.
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
